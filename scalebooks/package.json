{
  "name": "scalebooks",
  "private": true,
  "version": "0.1.0",
  "description": "Sentire Books — Philippine cloud accounting. Clean monorepo foundation (Postgres + TypeScript).",
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "db:generate": "pnpm --filter @scalebooks/db generate",
    "db:migrate": "pnpm --filter @scalebooks/db migrate"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.6.3"
  }
}
