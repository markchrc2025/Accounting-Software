// usePrivacyMode — thin hook re-exporting the PrivacyContext
// Import directly from PrivacyContext when you need the provider too.
export { usePrivacy as usePrivacyMode } from '../contexts/PrivacyContext.jsx';
